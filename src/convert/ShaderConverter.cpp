#include <candy/asset/convert/ShaderConverter.hpp>

namespace Candy::Asset
{
  bool ShaderConverter::Convert(const std::filesystem::path& inputSourceFile, const std::filesystem::path& outputFile)
  {
  
  }
}